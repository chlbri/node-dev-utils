{
  "name": "project1",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "pnpm run rm:lib && pnpm run rolldown",
    "rm:lib": "pnpx rimraf ./dist",
    "fmt": "oxfmt",
    "lint": "oxlint --fix && oxfmt",
    "test": "vitest run",
    "test:only": "vitest run --no-coverage",
    "pretest": "pnpm run build",
    "rolldown": "rolldown -c",
    "_ci": "pnpm run test"
  },
  "devDependencies": {
    "@bemedev/dev-utils": "workspace:^",
    "@vitest/coverage-v8": "4.1.9",
    "@vitest/ui": "4.1.9",
    "rolldown": "1.1.2",
    "typescript": "^6.0.3",
    "vitest": "4.1.9"
  }
}
