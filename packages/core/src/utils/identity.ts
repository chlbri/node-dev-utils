/**
 * Identity function returning the input value unchanged.
 *
 * @template `T` - Type of the input value.
 *
 * @param x - Input value.
 *
 * @returns The input value `x`.
 */
export const identity = <T>(x: T) => x;
